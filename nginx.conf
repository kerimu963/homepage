server {
    listen 80;
    server_tokens off;

    location = /hello-world {
        alias /usr/share/nginx/html/;
        index index.html;
        try_files index.html =404;
    }

    location / {
        return 404;
    }
}
