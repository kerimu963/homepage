services:
  hello-world:
    image: nginx:alpine
    ports:
      - "127.0.0.1:8001:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./html:/usr/share/nginx/html:ro
    restart: unless-stopped
